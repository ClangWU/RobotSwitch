frc_data = dlmread('record_data.txt');
frc.time           = frc_data(:, 1);
frc.data = frc_data(:, 2:11);

namelist = {'posy', 'posz', ''};
figure(1)
for i = 1:10
    plot(frc.time, frc.data(:, i), '. -g', 'linewidth', 0.1);
    legend('fb');
end